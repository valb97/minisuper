import { View } from "react-native";

export default function Dialog({active,message,type} : {active : boolean,message : string, type : string}){
    return (
        <View>
            
        </View>
    )
}